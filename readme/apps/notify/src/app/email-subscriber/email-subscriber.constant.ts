export enum EmailValidationMessage {
  EmailNotValid = 'The email is not valid',
  EmailUserNameIsEmpty = 'The user name is empty',
  EmailUserIdIsEmpty = 'The userId is empty',
  EmailSubscriberExists = 'The subscriber with same email already exists',
}

